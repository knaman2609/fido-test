{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A simple TypeScript Todo App",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npx serve . -p 3000",
    "start": "npm run build && npm run serve",
    "clean": "node --input-type=module -e \"import { rmSync } from 'fs'; try { rmSync('dist', {recursive: true, force: true}); } catch(e) { /* ignore */ }\""
  },
  "keywords": ["todo", "typescript", "app"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "serve": "^14.0.0"
  }
}
